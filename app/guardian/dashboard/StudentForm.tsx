'use client'

// import { supabase } from '@/lib/supabaseClient' // Removed Supabase import

export default function StudentForm({ onCreated }: { onCreated: () => void }) {
  return (
    <div className="text-center py-8">
      <p className="text-muted-foreground">Firebase integration coming soon...</p>
    </div>
  )
}
